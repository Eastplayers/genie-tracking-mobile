import Foundation

/// Constants for UserDefaults keys used for configuration persistence
struct UserDefaultsKeys {
    /// Key for storing the API key
    static let apiKey = "tracker_api_key"
    
    /// Key for storing the brand ID
    static let brandId = "tracker_brand_id"
    
    /// Key for storing the selected environment
    static let environment = "tracker_environment"
    
    /// Key for storing the user ID
    static let userId = "tracker_user_id"
}
